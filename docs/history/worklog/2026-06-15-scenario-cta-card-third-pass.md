# Scenario CTA card third pass

- Changed the proof copy from `именно по вашей свадьбе` to `именно про вашу свадьбу`.
- Moved the “Вы поймёте мой подход...” line out of the left text column and stretched it across the full CTA width to reduce the empty right-side feeling on desktop.
- Turned the reassurance line into a separate accent-colored signature line aligned to the right on desktop and reset it to a normal left-aligned flow on mobile.
- Rechecked the CTA layout behavior for desktop and mobile after the geometry change.

Verification:
- `npm run build`
- local desktop/mobile visual check
- live `/scenario/` marker check after deploy
