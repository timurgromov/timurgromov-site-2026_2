# 2026-06-15 - Scenario mid-article CTA added

## Summary
- Added one compact mid-article CTA to `/scenario/` to capture readers before the final closing block.

## Changes
- Inserted a soft CTA after the section where the article already explains guest comfort, toasts, and how the host manages the flow of the evening.
- Kept the CTA visually lighter than the final meeting block.
- Reused the same meeting route so the conversion path stays consistent.

## Verification
- `npm run build`
- `git diff --check`
- production verification on `https://timurgromov.ru/scenario/`
