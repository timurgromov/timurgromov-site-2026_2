# Scenario final meeting CTA closer

- Reworked the final `/scenario/` CTA into a dedicated meeting closer instead of a generic follow-up block.
- Replaced the old “Разобрать план в Telegram” copy with a meeting-focused offer and shorter decision-oriented text.
- Turned the right column into a compact action card with one primary CTA, secondary contact links, and three short proof points.
- Increased vertical separation between the CTA section and footer so the ending reads as a deliberate conversion block rather than a blurred tail.
- Reused the existing meeting channels already established on the main site: Telegram bot `site_meeting`, MAX, and phone.

Verification:
- `npm run build`
- local visual check on `/scenario/` desktop and mobile after the build
