# Scenario CTA card micro tightening

- Slightly tightened the CTA headline line-height on `/scenario/` for a cleaner desktop lockup.
- Reduced vertical whitespace between the full-width result line and the right-aligned reassurance note.
- Kept the reassurance note on the right for desktop composition, but pulled it closer to the main message so it no longer feels detached.
- Condensed the secondary contact links inside the action card by reducing spacing and font size one step.

Verification:
- `npm run build`
- local desktop/mobile visual check
- live `/scenario/` marker check after deploy
