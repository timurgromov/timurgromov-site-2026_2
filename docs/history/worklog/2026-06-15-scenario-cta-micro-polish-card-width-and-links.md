# Scenario CTA micro polish: card width and links

- Reduced the final CTA action card width on `/scenario/` by a small step to make the right column feel less dominant.
- Softened underline contrast for the secondary contact links (`Telegram`, `MAX`, `phone`) so the card reads calmer and more premium.

Verification:
- `npm run build`
- live `/scenario/` verification after deploy
