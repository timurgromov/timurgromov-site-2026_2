# Scenario CTA card second pass

- Reworked the final `/scenario/` meeting CTA away from the orange footer look by moving it into a separate light standalone panel above the footer.
- Replaced the vague heading with a clearer meeting offer and removed the `без навязывания` wording.
- Converted the left CTA copy from one long paragraph into a short structured list of what the meeting covers, followed by the result and reassurance lines.
- Rebuilt the proof-point list in the action card with stricter dot alignment and cleaner secondary contact layout.

Verification:
- `npm run build`
- live `/scenario/` marker check after deploy
